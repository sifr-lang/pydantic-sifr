[package]
name = "pydantic-sifr-fields-and-configuration"
version = "0.0.0"
edition = "2024"

[package.metadata.sifr]
manifest = "sifr.toml"

[dependencies]
pydantic-sifr = { path = "../.." }

[workspace]
